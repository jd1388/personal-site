import React from 'react';
import {render} from 'react-dom';
import {BrowserRouter} from 'react-router-dom';

export default class Test extends React.Component {
    render() {
        return (
            <p>This is a test of the routing!</p>
        );
    }
}